export type Notification = {
  id: number;
  title: string;
  body: string;
  read: boolean;
  receivedAt: string;
};

export type PlaceholderPost = {
  id: number;
  title: string;
  body: string;
};
